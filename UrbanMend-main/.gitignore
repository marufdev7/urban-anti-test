# Claude Code
CLAUDE.local.md
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.eggs/

# Django
*.sqlite3
staticfiles/
# Local MEDIA_ROOT output. Deliberately NOT `media/` — `media/` is a Django *app*
# (Arch §2.4) whose source must be tracked. Set MEDIA_ROOT to mediafiles/ in A4.
mediafiles/
local_settings.py

# Environment — .env.example IS committed (placeholders only)
.env
.env.local
.env.*.local
.env.production
.env.production.*
!.env.production.example

# Test / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# Celery beat schedule (local-only: worker runs `-B` locally via override)
celerybeat-schedule*

# T10.4 load-test artifacts. NOT merely noise: sessions.csv holds live session keys, which
# are bearer credentials for the seeded accounts until they expire (BR-33). The locust --csv
# result files land here too; the committed record of a run is docs/14-load-test-results.md.
.loadtest/

# Editors / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
